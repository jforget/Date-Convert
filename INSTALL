
Install the normal way:

perl Makefile.PL
make
make test
make install

Or use cpanm (CPAN-minus) or the CPAN shell.

Non-core dependencies (with respect to Perl 5.6.0)

-- Test::More (mandatory)
-- Test::Exception (optional).

